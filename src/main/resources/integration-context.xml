<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int-jms="http://www.springframework.org/schema/integration/jms"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
            http://www.springframework.org/schema/integration/jms http://www.springframework.org/schema/integration/jms/spring-integration-jms.xsd">

    <int:gateway service-interface="info.clashfard.jms.PrinterGateway" default-request-channel="inboundChannel" />

    <int:object-to-string-transformer input-channel="inboundChannel" output-channel="outboundChannel" />

    <int-jms:outbound-channel-adapter connection-factory="connectionFactory" channel="outboundChannel" destination="queue" />

    <int-jms:inbound-gateway connection-factory="connectionFactory" request-destination="queue" request-channel="jmsChannel" default-reply-destination="replyQueue"/>

    <!-- will invoke the receive method on our inputChannel -->
    <int:service-activator ref="jmsService" method="print" input-channel="jmsChannel" />

    <bean id="connectionFactory" class="org.springframework.jms.connection.CachingConnectionFactory">
        <property name="targetConnectionFactory">
            <bean class="org.apache.activemq.ActiveMQConnectionFactory">
                <property name="brokerURL" value="tcp://localhost:61616"></property>
            </bean>
        </property>
    </bean>

    <int:channel id="jmsChannel" />
    <int:channel id="inboundChannel" />
    <int:channel id="outboundChannel" />
    <int:channel id="replyChannel" />

    <bean id="replyQueue" class="org.apache.activemq.command.ActiveMQQueue">
        <constructor-arg value="reply.queue" />
    </bean>

    <bean id="queue" class="org.apache.activemq.command.ActiveMQQueue">
        <constructor-arg value="sample.queue" />
    </bean>

    <bean class="info.clashfard.service.impl.DefaultClashfardJmsService" id="jmsService" />





    <!--<int-jms:message-driven-channel-adapter-->
            <!--connection-factory="connectionFactory"-->
            <!--channel="jmsChannel"-->
            <!--destination="queue" />-->

</beans>